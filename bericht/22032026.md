# Schwachstellenanalyse MeinCMS (Stand 22.03.2026)

Dieses Dokument fasst die technischen Schwachstellen und Verbesserungspotenziale von MeinCMS zusammen, basierend auf einer statischen Codeanalyse am 22. März 2026.

## 1. Sicherheit & Authentifizierung

### 1.1 Schwache Passwort-Richtlinien
- **Problem:** Im `UserAdmin`-Tool ist die minimale Passwortlänge auf nur 6 Zeichen eingestellt (`options.Password.RequiredLength = 6`). Dies entspricht nicht modernen Sicherheitsstandards.
- **Risiko:** Erhöhte Anfälligkeit für Brute-Force-Angriffe auf den Administrator-Account.
- **Empfehlung:** Erhöhung auf mindestens 12 Zeichen und Erzwingen von Komplexitätsregeln (Sonderzeichen, Ziffern).

### 1.2 Fehlende Security Header
- **Problem:** Die Anwendung setzt keine modernen HTTP-Sicherheitsheader wie `Content-Security-Policy` (CSP), `X-Content-Type-Options`, `X-Frame-Options` oder `Referrer-Policy`.
- **Risiko:** Erhöhte Anfälligkeit für Cross-Site Scripting (XSS) (trotz Sanitizer), Clickjacking und MIME-Sniffing.
- **Empfehlung:** Konfiguration der Header in der Middleware-Pipeline in `Program.cs`.

### 1.3 Passwort-Echo im Admin-Tool
- **Problem:** Das `UserAdmin`-Tool liest Passwörter über `Console.ReadLine()` ein. Dabei wird das Passwort im Klartext im Terminal angezeigt.
- **Risiko:** Schulterblick-Angriffe (Shoulder Surfing) oder Mitlesen der Passworthistorie in Terminal-Logs.
- **Empfehlung:** Verwendung einer Methode, die die Eingabe maskiert oder nicht anzeigt.

### 1.4 HSTS Konfiguration
- **Problem:** `UseHsts()` wird mit dem Standardwert verwendet (meist 30 Tage).
- **Risiko:** Browser erzwingen HTTPS nur für einen kurzen Zeitraum.
- **Empfehlung:** Erhöhung des `max-age` auf ein Jahr (31536000 Sekunden) und Aktivierung von `includeSubDomains` sowie `preload`.

## 2. Datenbank & Datenintegrität

### 2.1 Fehlender Unique Index für Slugs
- **Problem:** Das Feld `Slug` in der Tabelle `WikiArtikels` besitzt keinen eindeutigen Datenbank-Index.
- **Risiko:** Bei gleichzeitigen Schreibzugriffen auf denselben neuen Slug können Dubletten entstehen (Race Condition). Dies führt zu Inkonsistenzen bei der Abfrage der "neuesten Version".
- **Empfehlung:** Hinzufügen eines `Unique`-Constraints per EF Core Migration.

### 2.2 Radikale Administrator-Verwaltung
- **Problem:** Das `UserAdmin`-Tool löscht beim Start alle Administrator-Accounts außer dem ersten gefundenen, um die "Ein-Admin-Regel" durchzusetzen.
- **Risiko:** Unbeabsichtigter Datenverlust von legitimen Administrator-Zugängen bei Fehlkonfiguration oder Team-Erweiterung.
- **Empfehlung:** Warnung ausgeben statt automatischer Löschung; Verwaltung über eine Liste/Menü.

## 3. Anwendungslogik & Codequalität

### 3.1 Fehlerbehandlung im PageController
- **Problem:** Der `Index`-Action im `PageController` liefert bei einem nicht existierenden Slug einfach `View(artikel)` zurück, wobei `artikel` dann `null` ist.
- **Risiko:** Abhängig von der Implementierung der View kann dies zu einer `NullReferenceException` oder einer leeren Seite ohne klare Fehlermeldung führen.
- **Empfehlung:** Explizite Prüfung auf `null` und Rückgabe von `NotFound()` oder Weiterleitung zum `Neuformular`.

### 3.2 Inkonsistente Namensgebung (Typo)
- **Problem:** In `Services/Blogs.cs` wird die Klasse als `Bloogs` (mit zwei 'o') deklariert.
- **Risiko:** Verwirrung bei der Entwicklung und potenzielle Fehler bei der Dependency Injection oder beim Aufruf.
- **Empfehlung:** Umbenennung in `Blogs`.

### 3.3 Fehlende Validierung von Kategorien
- **Problem:** Die neue `Kategorie`-Liste in `WikiArtikelVersion` wird ohne Validierung übernommen.
- **Risiko:** Speicherung von extrem langen Kategorienamen oder einer massiven Anzahl an Kategorien, was das UI sprengen oder die Datenbank Performance beeinträchtigen könnte.
- **Empfehlung:** Validierung der Kategorienanzahl und -länge im `PageService` oder Controller.

## 4. Infrastruktur & Wartbarkeit

### 4.1 Fehlende Tests
- **Problem:** Es existieren derzeit keine Unit- oder Integrationstests für die Kernlogik (insb. `PageService` und Slug-Validierung).
- **Risiko:** Regressionen bei zukünftigen Änderungen am Routing oder der Versionierung.
- **Empfehlung:** Einführung einer Test-Suite (xUnit/MSTest).

### 4.2 SQLite vs. PostgreSQL Inkonsistenz
- **Problem:** Die Dokumentation erwähnt SQLite, aber der Code und die Migrationen sind stark auf PostgreSQL (Npgsql) ausgerichtet (insb. die `Kategorie`-Property als `text[]`).
- **Risiko:** Entwickler, die versuchen, das Projekt mit SQLite zu starten, werden auf Datenbankfehler stoßen.
- **Empfehlung:** Vereinheitlichung der Datenzugriffsschicht oder klare Kennzeichnung der PostgreSQL-Exklusivität.
